#include <stdio.h>
/**
 * main- Print numbers from 0 to 9 and letters from a to f
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 0;
	char c = 'a';

	while (i < 10)
	{
		putchar(i);
		i++;
	}
	while (c <= f)
	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
